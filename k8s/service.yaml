apiVersion: v1
kind: Service
metadata:
  name: tourism-website-service
  labels:
    app: tourism-website
spec:
  type: NodePort
  selector:
    app: tourism-website
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
    protocol: TCP
